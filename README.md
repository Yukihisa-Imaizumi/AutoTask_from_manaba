# 環境構築
以下の準備が必要
```
pip install playwright python-dotenv
playwright install
```

# 設定
```setting.env
# 学籍番号（統一認証のIDを入れてください）
MANABA_USERNAME = your username
# 統一認証のパスワード
MANABA_PASSWORD = your password

```